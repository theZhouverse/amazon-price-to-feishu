# 2026-08-26

- 检查了项目健康状态与 Windows 定时任务（用户问"定时任务开始了没有"）。
- 定时任务清单（schtasks 被安全策略禁用，改用 PowerShell Get-ScheduledTask 写文件再读）：
  - `AmazonDaily_0730` / `AmazonDaily_1530`：周一~周五 07:30/15:30 跑 `bin/scheduled_run.bat`（weekly-run --confirm），今天注册的，明天首次正式触发。
  - `AmazonDaily_20260826_0700`：一次性任务，今早 07:00 跑成功（exit=0，51 分钟，725 项：ok=587 / crawl_error=103 / source_data_invalid=35，异常率超 10% 发了降级通知到飞书）。
  - `AmazonDaily_HTML_Server`：登录触发，从未运行过；HTML 服务实际由 weekly-run 的 python（PID 30284，`app/html_server.py --serve`）在 07:00:02 带起，一直在跑。
- **15:30 的运行失败**：15:30:01 触发，LastTaskResult=0xC000013A（STATUS_CONTROL_C_EXIT，控制台被关闭/Ctrl+C），整棵进程树无输出死亡，scheduler log 只有 START 无 END，run log 0 字节。明天 15:30 会重试。
- 锁机制（app/runtime_state.py exclusive_run）用 msvcrt 文件锁，进程死亡后 OS 自动释放，残留 lock 文件不会卡后续运行——确认无害。
- 遗留风险/建议：① 定时任务 LogonType=Interactive 有可见控制台窗口，窗口被关=运行被杀（今天 15:30 就是这么死的）；建议改为隐藏运行或"不管用户是否登录都运行"。② git 有 21 个改动文件未提交（基线 commit 21d6d59 之后全部未提交）。③ 一次性任务 AmazonDaily_20260826_0700 已完成可删。④ crawl_error 103/725（14%）偏高，SPEC/REVIEWS 已声明在线验收未完成。
- outputs/ 下我建的 5 个临时诊断文件（task_check*.txt / proc_*.txt）已删除。
- 16:33 用户确认 15:30 的窗口是自己误关的，要求重启。bash 直接跑 bat 失败（cmd //c 秒退）；PowerShell Start-Process 启动 .bat 也被安全策略拦截。最终方案：PowerShell 直接 `Start-Process .venv\Scripts\python.exe app\main.py --weekly-run --confirm`，-WindowStyle Hidden + 输出重定向到 outputs/scheduler_logs/2026-08-26_163324_manual.log（等效 ps1 第 16 行）。PID 4196，运行正常（weekly-run 20260826_163327, period=seq-2, 4 workers），预计 ~17:25 跑完自动发飞书。隐藏窗口方式顺带避免了再次误关。
- 16:35-16:55 全文件夹代码扫描（用户要求）。结果：compileall 全过；独立复跑 247/247 测试通过（30s）；config.json 无密钥（secret 只在 .env，经 config.py:165 注入）；磁盘 191G 余量充足；parser/sheet_io/result_notification 未发现问题。
- 扫描发现的代码问题（子代理 Explore 审查 + 本人验证）：
  1. 【高】app/amazon/crawler.py fetch_with_retry（421-464）丢失 tab rebuild 调用——rebuild() 定义还在（110行）但无调用点，是 15:19 部署 complete_fixes_20260826 时引入的回归（对比 outputs/code_backups/review_fixes_20260826 旧版 389/395 行有 `tab = self.rebuild(tab)`）。tab 崩溃后无法恢复，该 tab 后续所有 ASIN 连续 CRAWL_ERROR。未修复。
  2. 【中高】app/weekly_result.py:139 清空旧行封顶 2002 行（A3:A2000），358 行 asin_map 扫到 2000、405 行迁移 N2:P2000 同类截断——超 2000 行时旧数据残留/找不到 ASIN，与 TASKS 声称的 C26-08"解除2000行限制"矛盾。未修复。
  3. 【中】app/runtime_state.py:54-63 解锁抛 OSError 时跳过 handle.close()，锁与句柄均泄漏直到进程退出。未修复。
  4. 【中】htmls/ 22GB（仅 08-24、08-25 两天）永远不会被自动清理：cleanup_expired() 只在 html_archive_enabled=True 时执行（main.py:322-327），当前配置为 False。未处理。
  5. 【低】weekly_execution.py:97 归档代际兜底命名 generation-{N} 可互相覆盖；crawler.py:396 等处 datetime.now() 无时区。
- 注：今早 07:00 运行的 103 个 crawl_error 是旧代码（带 rebuild）跑出来的，与回归1无关；16:33 重启的运行用的是新代码（无 rebuild），目前 PD03 完成、PD17 进行中，状态正常。
- 项目由 codex 开发维护，历史会话在 C:\Users\Administrator\.codex\sessions（多为 guardian review transcript）。
