# Shared Feishu credentials are not stored in this project .env.
# Fill them only on the target host under:
# D:/projects/lykj-projects-map/runtime/credentials/feishu.credentials.env
# and inject them with the global project-credential-env.ps1 loader.

# Optional host-specific runtime overrides (leave blank unless explicitly
# validated on this Windows machine; they are project-specific settings only).
# AMAZON_PROXY=host:port
# AMAZON_CA_LOCATION_MODE=direct_no_postal
# AMAZON_US_LOCATION_MODE=proxy
# AMAZON_FEEDBACK_ENABLED=false
# AMAZON_HTML_ARCHIVE_ROOT=D:/projects/amazon_daily_structured_20260821/htmls
# AMAZON_HTML_ARCHIVE_ENABLED=false
# AMAZON_HTML_ARCHIVE_REQUIRED=false
# AMAZON_HTML_SERVER_ENABLED=false
# AMAZON_HTML_SERVER_BIND=0.0.0.0
# AMAZON_HTML_SERVER_PORT=8765
# AMAZON_WORKERS=4
