# The only local secret source file. Copy to .env in the project root.
# A system environment variable with the same name takes precedence.
FS_APP_SECRET=your_feishu_app_secret_here

# Optional host-specific runtime overrides (leave blank unless explicitly
# validated on this Windows machine; these are not substitutes for FS_APP_SECRET).
# AMAZON_PROXY=host:port
# AMAZON_FEEDBACK_ENABLED=false
# AMAZON_HTML_ARCHIVE_ROOT=D:/projects/amazon_daily_structured_20260821/htmls
# AMAZON_HTML_ARCHIVE_ENABLED=false
# AMAZON_HTML_ARCHIVE_REQUIRED=false
# AMAZON_HTML_SERVER_ENABLED=false
# AMAZON_HTML_SERVER_BIND=0.0.0.0
# AMAZON_HTML_SERVER_PORT=8765
# AMAZON_WORKERS=4
