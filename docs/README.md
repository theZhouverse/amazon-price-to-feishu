# 项目文档导航

| 文件 | 唯一用途 |
|---|---|
| [SPEC](SPEC.md) | 当前业务、流程图、架构、配置、操作和验收规则 |
| [TASKS](TASKS.md) | 当前任务、验证耗时和历史实施证据 |
| [REVIEWS](REVIEWS.md) | 当前尚未解决的代码审查意见 |
| [历史目录](history/README.md) | 被替代的旧版文档，仅用于追溯 |

阅读顺序：SPEC → TASKS当前索引 → REVIEWS未解决项。完整目录树及文档维护矩阵见SPEC第4节，正式命令和Windows调度见第18～19节。

当前代码模块架构图：[amazon_daily_architecture.html](amazon_daily_architecture.html)；对应可审查的架构规格：[amazon_daily_architecture.json](amazon_daily_architecture.json)。图按提交 `7c2ce802100c81f89ca691827b1a9a018262d58d` 的实际文件证据生成。

当前生产入口按SPEC执行周一07:30沿用、周一15:30切换、周二至周五稳态运行；本地工作区位于生产分支 `fix-codescan-20260826`。2026-09-10 最新隐藏窗口全量 run `20260910_160228` 已覆盖18个价格子表和两个Seller Central店铺Feedback：固定结果表基础/前端覆盖719行，489行通过写入、230行保留阻断；Feedback固定子表 `41u25y` 合并后写入10条并通过严格9列回读，店铺值为“冬豚/北蓉”，且已置于最后。HTML归档与局域网服务当前关闭，既有历史HTML不删除；固定结果表18个业务子表的22列表头已校准，空白 `Feedback????`/`Sheet1` 已删除（详见TASKS/REVIEWS最新节）。前端规则版本为 `2026-09-11-v16`：BSR只识别当前商品详情树中与请求ASIN同表绑定且当前DOM可见的 `Best Sellers Rank` 行，折叠隐藏行只留诊断不计存在；AC只识别当前商品绑定且可见的实际徽章（典型为 `span.a-size-small` 的 `Amazon's Choice`）；两者独立判定，同页同时存在时分别保留事实，不再互相覆盖或误报 `ASIN不合法`；开放Shadow DOM和隐藏/推荐区排除继续保留。P列可见标题为`前端尺寸`。2026-09-11 已将Windows调度拆为周一/周二至周五四个显式时段任务并传递`scheduled_slot`；当前四条 Windows 任务已按用户指令恢复为 `Ready/Enabled=True`，周一07:30/15:30下次执行为2026-09-14，周二至周五任务下次执行为2026-09-15。CA当前默认改为`direct_no_postal`，不输入邮编而直接读取`amazon.ca`页面价格，结果明确记录位置上下文方法；浏览器启动现使用受控auto-port、Chrome 152兼容参数并将启动/导航阶段写入`outputs/logs/run_*.log`；Feedback紫鸟店铺打开固定使用官方CLI `store open --headless`，不弹出可见窗口、不调用OS置顶/抢焦点API，以当前店铺会话作为页面操作上下文；Docker已取消并不在当前支持范围，详见TASKS与REVIEWS。

2026-09-15 15:30 的 `weekday_steady` 批次曾因旧逻辑按每日 `run_id` 重复复制 `seq-5` 周报而收到飞书 `504 Gateway Timeout`，失败发生在 Amazon 抓取和结果写入前；现已修复为稳态复用已确认快照，并增加中断 manifest 的历史 ready 恢复。真实重跑 `20260915_154728` 已复用原快照、完成 18 个子表，493 行写入且 493/493 回读通过，耗时 4849.125 秒，8/8 通知成功；228 行保留在恢复清单。

周报结构兼容规则：正式批次遍历快照中的全部子表，不受旧18表静态名单限制；源表的 ASIN、SKU、尺寸、正常售价、本周折扣形式/值和目标成交价按规范化业务表头定位，取第一处主字段，后段辅助区重复字段只审计不覆盖，插入或移动中间辅助列不会错位。`本周折扣%`同时保留源文本状态（如断货、无库存、区间值），数值折扣单独规范化用于计算。新增描述性子表必须有完整业务表头，并能从标题明确识别 US/CA 或从 ASIN 单元格提供单一 Amazon.com/Amazon.ca 域名证据才能自动路由；缺失字段、纯 ASIN 无站点线索或混合站点均在抓取前阻断并留 discovery 证据。结果表仍固定 A:V 顺序。

2026-09-14 的正式 `seq-5` 已只读确认 23 个子表（18 个业务表、5 个辅助表）。15:30 切换曾因旧版把 A1:P10 易变样例内容当作硬结构而安全停止；现已改为以 Sheet 标题/索引/行列容量的 `structure_shape_sha256` 校验副本完整性，内容 `sha256` 仅用于审计，随后仍必须通过动态业务表头和 Marketplace 门禁。seq-5 已创建的 pending 副本会在恢复时复用，不重复复制；完成只读映射前不进行本周固定结果表写入。

结构适配后的正式批次 `20260914_153004` 已于周一 15:30 完成全量：18 个业务子表、721 行基础数据，493 行结果写入并逐行回读，228 行按门禁阻断；CA 的身份不一致仍保留在恢复清单。固定结果表链接未变，结果名同步为 `Amazon周报前端价格捕捉_2026-W38_20260914_153004`，8 名协作者通知成功；本轮 Feedback 按时段规则跳过，HTML归档保持关闭。详细证据与耗时见 TASKS/REVIEWS 最新条目。

Feedback遇到Seller Central登录重定向时，默认只在关闭当前店铺上下文后重开一次后台紫鸟会话并随机等待5～10秒；验证码、风控、权限、Keychain/API Key错误不自动重试，失败只影响该店并留证，不中止另一店或价格发布。Feedback列 I 使用父任务执行开始时间。

[操作手册](操作手册.md)、[当前业务规则](当前业务规则.md)、[交付清单](交付清单.md)仅保留旧链接跳转，不单独维护规则。代码/配置变化同步SPEC及相关模板，实施和测试写TASKS，问题解决后关闭REVIEWS条目；不要在多个文档复制同一套业务说明。

生产/开发目录边界、Junction风险和发布顺序见[SPEC第20.1节](SPEC.md#201-生产目录与开发目录隔离)；不要把`D:\projects\amazon_daily`当作独立开发目录。
