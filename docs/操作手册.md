# 操作说明入口

当前操作统一见[SPEC第18节：CLI、运行产物与故障处理](SPEC.md#18-当前cli产物与故障处理)、[SPEC第16.1节：周一换周时点规则](SPEC.md#161-周一换周时点规则)及[SPEC第19节：Windows调度与验收](SPEC.md#19-windows调度与验收)。周一07:30沿用上一周，周一15:30切换最新周报；本文件不复制具体命令，避免与SPEC产生两套口径。

目录和配置见SPEC第3～4节；当前风险见[REVIEWS](REVIEWS.md)。本文件只保留旧路径兼容，不复制命令、调度时间或业务规则，避免不同版本冲突。

生产目录与开发副本的隔离、Junction禁用和发布顺序见[SPEC第20.1节](SPEC.md#201-生产目录与开发目录隔离)。

生产只支持 Windows 宿主机本地运行：使用项目 `.venv`、本机 Chromium/紫鸟会话和 `bin\schedule.ps1` 安装的隐藏计划任务。不得以容器、Docker 或第二套调度器替换当前任务；跨设备迁移时复制代码和受控运行数据后，须在新宿主机重新执行离线、US/CA 单点和最小写入验收。

此前操作说明已保存至[历史目录](history/README.md)，不得照旧文档运行。
